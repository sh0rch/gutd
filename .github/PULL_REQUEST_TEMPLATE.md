## What

Brief description of the change.

## Why

Problem or motivation. Link to issue if applicable: #

## How

Key implementation details (BPF changes, config changes, new dependencies, etc.)

## Testing

- [ ] `cargo fmt --check` passes
- [ ] `cargo clippy` passes (no warnings)
- [ ] `cargo build` succeeds
- [ ] `sudo bash tests/integration-wg.sh` passes
- [ ] Tested on real hardware (if applicable)

## Notes

Anything reviewers should pay attention to (verifier limits, performance impact, breaking changes).
